# EV Charging Industry Analysis - Python Dependencies
# This file lists all dependencies used in the project
# The actual dependencies are managed via pyproject.toml

# Core dependencies for the EV analysis application:
beautifulsoup4>=4.13.4
flask>=3.1.1
jinja2>=3.1.6
matplotlib>=3.10.5
numpy>=2.3.2
requests>=2.32.4
trafilatura>=2.0.0

# Additional data analysis libraries (available but optional for core functionality):
pandas>=2.3.1
plotly>=6.3.0
seaborn>=0.13.2
weasyprint>=66.0

# To install these dependencies in a fresh environment, you can use:
# pip install -r project-requirements.txt

# Note: This project uses pyproject.toml for dependency management
# All dependencies are already installed and configured